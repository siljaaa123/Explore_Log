{
  "scripts": {
    "preinstall": "npm install puppeteer-core", "postinstall": "npx puppeteer browsers install chrome",
    "heroku-postbuild": "mkdir ./.cache && mv /app/.cache/puppeteer ./.cache"
  },
  "dependencies": {
    "@rails/actiontext": "^7.1.3-2",
    "interactjs": "^1.10.27",
    "puppeteer": "^22.9.0",
    "trix": "^2.1.1"
  }
}
